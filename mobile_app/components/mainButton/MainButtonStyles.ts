import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    flex: 1,
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
    gap: 10,
    width: "100%",
    marginVertical: 10,
    // height: 10,
    maxHeight: 50,
    borderRadius: 10,
    backgroundColor: "rgba(0,74,222,0.75)",
  },
});
